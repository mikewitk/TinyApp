<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>TinyApp</title>
</head>
<body style="font-size:20px; padding:50px;">

<header>
    <% include ./partials/_header %>
</header>

<% if ( userID ){ %>
<a href="/urls/new">Create New TinyURL</a>
  <% for( var key in urlDatabase) { %>
  <form method="POST" action="">

    <% if ( key === URLkey){ %>
      <h3>Short URL: <%= key %></h3>
      <h3>Long URL: <%= urlDatabase[key].longURL %> </h3>
      <label for="longURL">Update your Long URL:</label>
      <input type="text" name="longURL">
      <button type="submit" class="btn btn-info">Update</button>
  </form>
    <% } else { %>
    <% "The short URL you are trying to access does not exist. Please verify the URL and try again." %>
    <% } %>
  <% } %>
<% } else { %>
    <% return "To be able to edit a TinyApp URL you need to login first. Please visit: localhost:8080/login" %>
<% } %>
</body>
</html>